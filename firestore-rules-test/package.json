{
  "name": "sikerfuzet-firestore-rules-test",
  "private": true,
  "type": "module",
  "description": "A firestore.rules ellenőrzése a Firestore emulátoron.",
  "scripts": {
    "emulator": "firebase emulators:start --only firestore --project sikerfuzet-test",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
